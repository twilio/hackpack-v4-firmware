{
  "name": "comms",
  "version": "1.0.0",
  "description": "Install Node (will need to script this)",
  "main": "comms.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.88.0"
  }
}
